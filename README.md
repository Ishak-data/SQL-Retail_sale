# SQL-Retail_sale
This project analyzes retail sales data using SQL to generate business insights. It includes data cleaning, exploration, and solving real-world problems like sales trends, customer behavior, and top-performing categories using aggregation, filtering, and window functions.
